# Quoted - Voice to Quote for Contractors
# Requirements

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# AI/ML
anthropic>=0.40.0  # Requires 0.25+ for tool calling
openai==1.12.0  # For Whisper transcription

# PDF Generation
reportlab==4.1.0
weasyprint==61.0

# Database
sqlalchemy==2.0.25
aiosqlite==0.19.0
asyncpg==0.29.0  # PostgreSQL async driver
psycopg2-binary==2.9.9  # PostgreSQL sync driver

# Authentication
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.0.1  # Pin bcrypt for passlib compatibility
python-jose[cryptography]==3.3.0  # JWT tokens

# Utilities
pydantic==2.6.0
email-validator==2.1.0  # Required for Pydantic EmailStr
pydantic-settings==2.1.0
python-dotenv==1.0.1
httpx==0.26.0
aiofiles==23.2.1
jinja2==3.1.3

# Rate Limiting
slowapi==0.1.9

# Audio processing
pydub==0.25.1

# Email
resend==2.4.0

# Payments
stripe==11.1.1  # Stripe payment processing

# Testing
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.21.0
